# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
.pytest_cache/
*.db

# Env / secrets
# A bare `.env` matches at any depth, but NOT the suffixed variants Vite and
# friends encourage (.env.local, .env.production), so cover those too — then
# re-admit the checked-in template.
.env
.env.*
backend/.env
!.env.example
!*.env.example
.konaos_session_cache.json
.brand_ids_cache.json

# Node
node_modules/
frontend/dist/

# Editor / OS
.DS_Store
.idea/
.vscode/
*.log
